# 多平台自动找号助手｜安装说明

当前 V1.0 已启用抖音推荐流，采集结果写入研究台（No Swipe / Supabase）。后续平台通过独立适配器接入。

- 上传接口：`https://kigrzhmcphrkqtuqthwb.supabase.co/functions/v1/ingest`
- 研究台：`https://whislte.cc.cd`
- 契约：No Swipe observation batch `contract_version = 2`
- 每条刷到的推荐流观察都会上传；命中规则的账号会进入研究台达人库

## 从发布包安装

1. 确认自己能登录研究台 `https://whislte.cc.cd`。
2. 解压发布 ZIP。Chrome 扩展管理页开启“开发者模式”，点击“加载已解压的扩展程序”，选择解压后包含 `manifest.json` 的文件夹。
3. 打开插件侧栏，点击“连接研究台”。
4. 在打开的研究台配对页登录并确认授权；侧栏显示“已连接”后即可使用。

## 从源码开发

1. 使用 Node.js 22 和 pnpm 10。
2. 在项目目录运行 `pnpm install --frozen-lockfile`。
3. 运行 `pnpm build`。
4. Chrome 只加载项目中的 `dist` 文件夹，不加载源码根目录。

运行 `pnpm package` 会在 `release` 目录生成可交付 ZIP。发布包只包含扩展运行文件，不包含测试、设计稿、开发脚本、后端代码和依赖目录。

## 日常使用

Chrome 和电脑必须保持运行、唤醒；抖音账号也必须处于登录状态。需要只测试判断时再勾选“测试模式（不上传）”。

如果提示授权失效，到研究台账户菜单确认设备未被吊销，然后在侧栏重新连接。

本地加载的未打包扩展需要重新构建并在 `chrome://extensions` 点击刷新。Chrome Web Store 版本发布后由 Chrome 自动更新；实际跨版本升级时会自动打开内置更新说明页。
