# 后台运行与恢复

适用目录：`multiplatform-creator-finder`。改动基于 V1.0.6，发布版本为 V1.0.7；V0.1.18 保持原样。

## 运行方式

任务继续使用原有标签页。定时任务创建后台标签，手动任务启动后可切换标签或使用其他软件。

- 启动及巡检时，通过现有调试连接应用焦点模拟、恢复页面活动状态，并关闭该标签的自动丢弃。失败会重试一次，持续失败则暂停并显示原因。
- 页面内的等待通过消息交给扩展后台执行，每次消息等待最多 30 秒；长等待按同一个绝对截止时间分段完成。页面保留读取、点击和实际切换确认职责。
- `draState.runtime` 保存当前步骤、步骤截止时间、视频标识、计划等待时间、实际耗时、最近读取和成功切换时间。进程恢复时重建巡检和停止闹钟。
- 每次恢复使用独立的 `loopId`。旧循环的等待和监听器被取消，后台按 Tab、任务和循环三项标识拒绝过期操作。
- 巡检在步骤超过预算及 15 秒宽限后尝试恢复；连续两次恢复仍无成功切换则暂停。30 秒周期的 Chrome 闹钟用于巡检兜底，实际触发仍受浏览器和系统调度影响。
- 暂停或停止时取消等待、释放保活，并把中断的未确认翻页记录保留为未知结果，避免伪造成功或永久滞留上传队列。
- 侧栏展示真实的浏览器标签/窗口前后台状态，而不是焦点模拟后的 `document.visibilityState`。

运行时长使用真实经过时间。读取资料、重试和上传所耗时间均计入本轮时长；单条视频的配置等待时间与完整处理周期分别记录。

## 验证方法

常规检查：

```sh
pnpm check
```

真实浏览器回归使用独立的 Chrome for Testing 配置目录和 `dist`，不向浏览器传入关闭后台降频的参数。仅将推荐页的网络响应替换为本地 DOM 样本，扩展 Service Worker、内容脚本、调试连接、计时、鼠标/键盘输入和存储均真实运行。测试期间关闭正式上传及“不感兴趣”操作。

```sh
CHROME_BIN='/path/to/Google Chrome for Testing' \
SOAK_MINUTES=30 \
RUNTIME_REPORT="$PWD/test-results/background-runtime.json" \
node scripts/test-background-browser.mjs
```

追加后台进程中断测试：

```sh
CHROME_BIN='/path/to/Google Chrome for Testing' \
SOAK_MINUTES=0.1 STOP_WORKER=1 \
RUNTIME_REPORT="$PWD/test-results/background-worker-restart.json" \
node scripts/test-background-browser.mjs
```

测试覆盖后台标签、最小化窗口、恢复窗口、暂停、刷新保留截止时间、调试连接断开、冻结页面恢复、长等待期间到点停止。进程中断测试以恢复真实切换且保留任务/截止时间为通过标准。

`LIVE_PROBE=1` 只检查真实抖音页面读取；`LIVE_RUN=1` 从定时入口进行真实页面实跑。这两种模式使用独立配置，不继承个人浏览器登录状态。遇到登录或验证弹窗，应由用户完成，测试不得将本地样本通过等同于已登录抖音的端到端通过。

## 本次验证结果

验证日期：2026-09-05。浏览器为 Chrome for Testing 151.0.7922.34。

| 检查 | 结果 |
| --- | --- |
| `pnpm check` | 52 项测试通过，类型检查、构建及 `dist` 文件校验通过，版本 V1.0.6 |
| 后台持续运行 | 连续 1,802 秒，完成 296 条读取；期间包含超过 5 分钟的窗口最小化，无停滞恢复 |
| 后台切换间隔 | 295 个间隔，最短 6.012 秒，P95 为 6.100 秒，最长 6.433 秒 |
| 前台对照 | 4 个间隔，P95 和最长均为 6.042 秒；此短样本仅用于计时对照 |
| 最终构建恢复回归 | 后台计时、暂停取消、刷新保留任务和截止时间、连续刷新保留原始观察时间、调试重连、页面解冻、后台进程重启及长等待到时停止均通过 |
| 到时停止 | 长测记录比截止时间晚 2 毫秒，最终构建回归为 1 毫秒；这是本次观测值，系统调度仍可能造成延迟 |
| 侧栏状态 | 预览中确认显示当前步骤“等待切换” |

本地样本设置每条等待 5 秒，完整切换周期还包含读取、输入和切换确认，因此约 6 秒属于完整处理耗时。此次后台样本未出现等待被拉长到分钟级的现象。

长测在持续运行机制完成后启动。期间补充了“页面不回应停止时限制等待时间”和“连续刷新保留原始观察开始时间”两项修正；修正后的 V1.0.6 构建另行通过全部恢复回归，记录中的后台、内容脚本和清单哈希已与当时的 `dist` 核对一致。长测与恢复回归分别保留哈希，修正后的构建本身尚未重复完整 30 分钟测试。

2026-09-06 发布 V1.0.7，同步版本元数据和更新页面，沿用上述运行逻辑。浏览器原始记录保留测试时的 V1.0.6 版本信息；发布包单独执行常规检查和归档内容校验。

原始记录：[30 分钟后台长测](test-results/background-runtime.json)、[最终构建回归](test-results/final-build-regression.json)、[前台对照](test-results/active-tab-baseline.json)、[侧栏截图](test-results/background-status.png)。

真实抖音页面测试使用独立未登录配置，可以读取推荐视频，但登录弹窗阻碍资料读取和视频切换。运行约 181 秒后，扩展按异常策略暂停。已登录账号下的真实采集端到端验收仍待完成；本次本地 DOM 样本结果证明的是扩展在真实浏览器中的后台计时与恢复行为。详见[真实页面限制记录](test-results/live-douyin-limit.json)。

## 使用边界

电脑保持唤醒、Chrome 正常运行及页面网络可用，是后台执行的前提。关闭浏览器、手动休眠、合盖引发的休眠和断网需要恢复处理。此实现针对后台执行、计时和可恢复性，实际吞吐量还取决于抖音页面加载及账号登录状态。

修改后，在 Chrome 扩展管理页重新加载 `dist` 对应扩展，并刷新已经打开的抖音页面，确保后台和内容脚本采用同一套消息协议。
